# Kern — specification index

The full product requirements and technical specification live in:

**[Kern PRD.md](./Kern%20PRD.md)**

Use that file for types, database schema, architecture, UI/UX, component patterns, MCP details (e.g. section 19.x), and non-goals. Section numbers in [Phases.md](./Phases.md) refer to the same document.

This stub exists so tooling and prompts can consistently reference `KERN_PRD_SPEC.md` without duplicating the spec.

**Rule:** Do not deviate from the schema or architecture in the PRD unless the project owner explicitly overrides it.
