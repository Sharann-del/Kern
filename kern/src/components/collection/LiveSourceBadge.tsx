/** Placeholder — live source status (Phase 2). */
export function LiveSourceBadge() {
  return (
    <span className="rounded-kern-md border border-kern-border bg-kern-surface px-2 py-0.5 text-xs text-kern-text-3">
      Live source
    </span>
  );
}
